---
description: Test setup & running instructions for React
---

# Testing (ReactJS)

We recommend using tools like `vitest` and `@testing-library/react` for comprehensive UI test coverage.

// turbo-all

1. Run the headless testing suite:
   pnpm test

2. During development, you can use watch mode:
   pnpm test:watch
