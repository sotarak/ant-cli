---
description: How to run the local development server
---

# Local Development

// turbo-all

1. Install all dependencies:
   pnpm install

2. Start the hot-reload dev server:
   pnpm dev

3. Note: The application usually runs on `http://localhost:5173`. Open it in the browser or Antigravity browser agent to verify everything looks correct.
